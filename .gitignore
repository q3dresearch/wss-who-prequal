# Credentials. NEVER commit these — an API key is often a billing credential.
.env.local
.env
*.pem
*.key

# Quarantined responses are triage material — they live in CI artifacts for
# 90 days, never in git. Bad bytes must not enter the archive.
quarantine/

__pycache__/
*.pyc
.venv/
